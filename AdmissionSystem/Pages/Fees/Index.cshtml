@page
@model AdmissionSystem.PagesFees.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Fees[0].FeesAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fees[0].FeesType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fees[0].AcademicYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Fees) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FeesAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FeesType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AcademicYear.AcademicYearID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.FeesID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.FeesID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.FeesID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
